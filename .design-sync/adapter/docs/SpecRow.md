---
category: Cards
---
One label/value spec line (muted key left, value right). ProductCard renders these from its `specs` prop; standalone use belongs inside a `.product-card__specs` block.

```jsx
<SpecRow label="Material" value="Brass" />
```
